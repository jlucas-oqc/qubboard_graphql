# API Reference

The `qupboard_graphql.api` package contains all HTTP-facing code: the FastAPI application factory,
the REST router, the GraphQL router and type declarations, and the root/health-check routes.
