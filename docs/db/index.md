# Database

The `qupboard_graphql.db` package contains everything that touches the database: the SQLAlchemy
declarative base, ORM models, session and engine management, the repository mixin, and the
bidirectional Pydantic ↔ ORM mapper utilities.
