# Schemas

The `qupboard_graphql.schemas` package contains the Pydantic model hierarchy that defines the shape
of a hardware model calibration record. These schemas are used for REST request/response validation
and as the source of truth that the ORM mapping layer converts to and from.
